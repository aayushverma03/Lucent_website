[project]
name = "lucent-waitlist"
version = "0.1.0"
description = "Lucent pre-launch waitlist API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "boto3>=1.35",
    "email-validator>=2.2",
]
